package com.db1.db1start;

public class Matematica {
	public static Double menorNumero(Double numero, Double numerob) {
		if(numero != 0) {
			return Math.min(numero, numerob);
		}
		return (double) 0;
	}
	public static Double menorNumero2(Double numero, Double numerob,Double numeroc ) {
		if(numero != 0) {
			return Math.min(numero, numerob);
		}
		return (double) 0;
	}
}
